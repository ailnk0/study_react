import { expect, test } from '@jest/globals'

beforeEach(() => {})
afterEach(() => {})

test('1+2=3', () => {
  expect(1 + 2).toBe(3)
})

test('1-2=-1', () => {
  expect(1 - 2).toBe(-1)
})
